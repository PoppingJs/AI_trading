services:
  api:
    build: .
    env_file:
      - .env.example
    ports:
      - "8000:8000"
    command: ["uvicorn", "ai_trading.api:create_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]
